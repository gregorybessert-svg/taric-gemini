# email_config.env
# ========================
# SMTP-Konfiguration für send_link.py

# Variante A – Proton Mail Bridge (empfohlen auf deinem Mac, wenn du Bridge installiert hast)

# Beispiel: Proton Mail Bridge (lokal auf deinem Mac)
# Bridge stellt SMTP auf 127.0.0.1:1025 bereit.

SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=iqreg@proton.me
SMTP_PASSWORD=DEIN_BRIDGE_SMTP_PASSWORT
SMTP_FROM=iqreg@proton.me
SMTP_USE_TLS=1

# Variante B – Direkt über Proton SMTP mit SMTP-Token
# Alternative: Direktes Proton-SMTP mit SMTP-Token
# (nur Beispiel, Werte aus deinem Proton-Account übernehmen):

# SMTP_HOST=smtp.proton.me
# SMTP_PORT=587
# SMTP_USER=SMTP_USERNAME_AUS_TOKEN
# SMTP_PASSWORD=SMTP_TOKEN_PASSWORT
# SMTP_FROM=iqreg@proton.me
# SMTP_USE_TLS=1

